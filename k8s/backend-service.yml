
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: hotelapp
spec:
  selector:
    app: hotelapp-backend   # must match pod label exactly
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
